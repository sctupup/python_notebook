map(func,seq1[,seq2……])： 第一个参数接收一个函数名，后面的参数接收一个或多个可迭代的序列，将func依次作用在序列seq1[，seq2……]的每个元素得到一个新的元素
reduce(function,sequence[,initializer])【需要import functools】
filter(func,iterable)用于过滤序列，即用函数filter（）过滤掉iterable序列中不符合条件的元素，返回由符合条件元素组成的新序列
